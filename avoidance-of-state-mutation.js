// Avoiding state mutation using map
const numbers = [1, 2, 3];
const doubledNumbers = numbers.map(num => num * 2);

// Avoiding state mutation using filter
const evenNumbers = numbers.filter(num => num % 2 === 0);
