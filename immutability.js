// Immutable data using spread operator
const numbers = [1, 2, 3];
const updatedNumbers = [...numbers, 4];

// Immutable data using Object.assign
const person = { name: 'Alice', age: 25 };
const updatedPerson = Object.assign({}, person, { age: 26 });
